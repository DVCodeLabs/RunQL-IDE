diff --git a/build/lib/fetch.ts b/build/lib/fetch.ts
index 6fddf24..8a81ed6 100644
--- a/build/lib/fetch.ts
+++ b/build/lib/fetch.ts
@@ -103,8 +103,9 @@ const ghApiHeaders: Record<string, string> = {
 	Accept: 'application/vnd.github.v3+json',
 	'User-Agent': 'VSCode Build',
 };
-if (process.env.GITHUB_TOKEN) {
-	ghApiHeaders.Authorization = 'Basic ' + Buffer.from(process.env.GITHUB_TOKEN).toString('base64');
+const githubToken = process.env.GITHUB_TOKEN || process.env.GH_TOKEN;
+if (githubToken) {
+	ghApiHeaders.Authorization = `Bearer ${githubToken}`;
 }
 const ghDownloadHeaders = {
 	...ghApiHeaders,
